# ResNet18 specific configuration

model:
  name: "resnet18"
  pretrained: true
  num_classes: 2
  dropout: 0.1

training:
  batch_size: 4
  epochs: 30
  learning_rate: 0.0001
  weight_decay: 0.01
  optimizer: "adam"

xai:
  gradcam:
    target_layer: "layer4"  # Last conv layer
